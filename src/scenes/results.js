/**
 * Stackr Quest — Results Scene
 *
 * Shown after completing or failing a level. Displays score,
 * star rating, coins earned, and next/retry options.
 *
 * This is a placeholder — full implementation in Phase 3.
 */

export const resultsScene = {
  id: 'results',

  enter(_params, _ctx) {
    // params: { world, level, score, stars, coins, passed }
    // TODO Phase 3: display results screen
  },

  exit(_ctx) {
    // TODO Phase 3: cleanup
  },
};
