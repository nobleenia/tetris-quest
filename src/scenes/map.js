/**
 * Stackr Quest — World Map Scene
 *
 * Displays the world/level selection map. Players pick a world,
 * then a level within it. Shows star counts, lock states, and
 * world theme previews.
 *
 * This is a placeholder — full implementation in Phase 3.
 */

export const mapScene = {
  id: 'map',

  enter(_params, _ctx) {
    // TODO Phase 3: render world map with progress data
  },

  exit(_ctx) {
    // TODO Phase 3: cleanup
  },

  onRoute(_params, _ctx) {
    // TODO: handle world param changes (e.g., #/map/3)
  },
};
